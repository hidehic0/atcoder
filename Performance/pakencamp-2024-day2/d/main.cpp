#include <bits/stdc++.h>
using namespace std;

using ll = long long;

ll N,Q,L,R;

int main() {
  cin >> N >> Q;  
  // if (Q!= 1) {
  //   return 0;
  // }

  vector<ll> W(N);

  for (ll i=0;i<N;i++) {
    cin >> W[i];
  }

  while (Q--) {
    cin >> L>>R;
    L--;R--;

    multiset<ll> S;
  }
}
